{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Nuri Preview Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "${workspaceFolder}/../.."
      ],
      "preLaunchTask": "build Nuri PreviewHost"
    }
  ]
}
